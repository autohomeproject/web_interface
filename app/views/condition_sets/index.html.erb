<h1>Listing condition_sets</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @condition_sets.each do |condition_set| %>
  <tr>
    <td><%= condition_set.name %></td>
    <td><%= condition_set.description %></td>
    <td><%= condition_set.user_id %></td>
    <td><%= link_to 'Show', condition_set %></td>
    <td><%= link_to 'Edit', edit_condition_set_path(condition_set) %></td>
    <td><%= link_to 'Destroy', condition_set, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Condition set', new_condition_set_path %>
